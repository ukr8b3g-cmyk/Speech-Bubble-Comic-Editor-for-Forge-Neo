# Quick Retouch 0.7.5 ローカル確認ガイド

## 入替

1. Forge NeoとComic Panel Editorの別ウィンドウを完全に終了します。
2. 既存の`extensions/Speech-Bubble-Comic-Editor-for-Forge-Neo`を別の場所へバックアップ後、削除します。
3. ZIPを展開し、`Speech-Bubble-Comic-Editor-for-Forge-Neo`フォルダーを`extensions`直下へ配置します。
4. 同じ拡張が別名フォルダーで二重に存在しないことを確認します。
5. Forge Neoを起動し、Comic Panel Editorを開いて`Ctrl+F5`を実行します。
6. Quick Retouchの見出しに`0.7.5`が表示されることを確認します。

## ベース画像

1. Quick Retouchを開きます。
2. Layersの最下段が「ベース画像／画像レイヤー」であることを確認します。
3. 初期状態の鍵が解除表示であることを確認します。
4. 目アイコンで表示／非表示が切り替わることを確認します。
5. ベース画像を選択して消しゴムで消し、市松模様の透明部分ができることを確認します。
6. 鍵をロックして描画できないこと、再度解除して描画できることを確認します。
7. ベース画像を複製すると、新しいペイントレイヤーが作成されることを確認します。
8. ベース画像の削除・上下移動が無効であることを確認します。

## 選択表示と保護範囲

1. 魔法の杖または矩形選択で範囲を作成します。
2. 初期表示が青／シアンの半透明マスクであることを確認します。
3. 「境界線」「マスク」「非表示」を切り替え、非表示でも選択データが残ることを確認します。
4. 目など変更したくない場所を選択し、「選択を保護へ追加」を押します。
5. 保護範囲が赤く表示されることを確認します。
6. 画像全体を選択してベース画像を消しゴムで消し、赤い保護範囲だけ残ることを確認します。
7. 「保護から削除」と「保護を解除」を確認します。

## 自動選択（魔法の杖）

1. 許容値を低くして近い色だけが選択されることを確認します。
2. 許容値を上げ、選択される色の範囲が広がることを確認します。
3. 「連続領域のみ」をON/OFFし、つながった領域だけ／画像全体の近似色が切り替わることを確認します。
4. 「表示レイヤーを参照」をONにし、表示中レイヤーの合成結果から選択されることを確認します。
5. 「アンチエイリアス」をONにし、選択境界が1px程度なだらかになることを確認します。

## 色域選択

1. 基準スポイトで色を取得します。
2. 追加スポイトと除外スポイトを使用します。
3. 確定前候補が紫、確定後の選択が青／シアンで表示されることを確認します。
4. 許容値、連続領域、表示レイヤー参照を切り替えます。

## 色相・彩度

1. 目を選択して保護範囲へ追加します。
2. 服や装飾の黄色を色域選択します。
3. H/S調整レイヤーを追加します。
4. 編集対象を「黄色系」にして色相・彩度・明度を変更します。
5. 目の保護範囲に効果がかからず、選択した黄色へ効果がかかることを確認します。
6. 対象色スポイト、追加スポイト、除外スポイトを使い、カスタム色域を調整します。
7. 色域幅と境界の柔らかさを変更します。

## 状態復元

1. 選択範囲、Layers、Propertiesパネルを移動・リサイズ・最小化します。
2. Quick Retouchを閉じて再度開き、位置と状態が復元されることを確認します。
3. Quick Retouchを最大化して閉じ、再度開いたとき最大化状態が復元されることを確認します。
4. 通常表示へ戻して位置とサイズを変え、再度開いたとき通常位置・サイズが復元されることを確認します。

## 適用と復元

1. 「元画像を表示（長押し）」でQuick Retouch開始時の画像が表示されることを確認します。
2. 「一枚画像へ適用」または「新しいページ画像として適用」でPNGが追加されることを確認します。
3. ページ画像一覧に元の画像が残ることを確認します。
4. Undo／Redoでベース画像の画素、表示、ロック、保護範囲が復元されることを確認します。
