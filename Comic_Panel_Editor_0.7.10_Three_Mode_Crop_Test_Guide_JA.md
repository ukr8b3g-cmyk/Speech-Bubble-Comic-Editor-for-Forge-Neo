# Comic Panel Editor 0.7.10 3モード画像クロップ テストガイド

## 1. 一枚画像

1. 一枚画像モードで画像レイヤーを選択します。
2. Propertiesの「クロップを編集」、画像ダブルクリック、Cキーの3経路を確認します。
3. 一枚画像以外（テキストなど）を1つ選択した状態でCキーを押しても、別の画像へ勝手にクロップが入らないことを確認します。
4. 縦長画像を読み込んだ後に横長画像へ差し替え、Canvasが新画像の縦横比へ追従することを確認します。

## 2. 4コマ

1. 4コマモードで画像をコマへ配置し、レイヤー一覧のコマ画像を1枚選択します。
2. Propertiesに「クロップを編集」「クロップをリセット」が表示されることを確認します。
3. 「クロップを編集」を押し、専用オーバーレイ上で四隅・辺の8ハンドルをドラッグします。
4. 枠内をドラッグしてクロップ範囲自体を移動できることを確認します。
5. Enterまたは「確定」で適用し、コマ画像が選択した範囲から描画されることを確認します。
6. Escまたは「キャンセル」で変更されないことを確認します。
7. 「クロップをリセット」で元画像全体へ戻ることを確認します。
8. Ctrl+Z / Ctrl+Yでクロップ確定・リセットをUndo/Redoできることを確認します。
9. コマ画像を選択してCキー、またはコマ画像をダブルクリックして同じクロップ編集が開くことを確認します。

## 3. Comic

1. Comicモードでコマへ画像を配置し、コマ画像レイヤーを1枚選択します。
2. Propertiesに「クロップを編集」「クロップをリセット」が表示されることを確認します。
3. 4コマと同じクロップオーバーレイ、確定／キャンセル／リセット、Enter／Escを確認します。
4. Cover / Contain、画像倍率、X/Y位置調整がクロップ後も動作することを確認します。
5. Cキーと画像ダブルクリックからもクロップ編集へ入れることを確認します。

## 4. 保存・復元

1. 4コマまたはComicでクロップを適用します。
2. プロジェクトを保存して再読込します。
3. コマ画像のクロップ範囲が保持されることを確認します。
4. 新しい画像を同じコマへ割り当てた場合は、以前のクロップを引き継がず元画像全体から開始することを確認します。

## 5. 複数画像選択

複数のコマ画像を選択している場合、クロップ編集は無効であることを確認します。クロップは1画像ずつ編集します。
