# Quick Retouch 0.7.8 コンパクト操作・画像追加レスポンス テストガイド

1. 自動選択・色域選択のチェックボックスが16×16pxで、オプションバーからはみ出さないことを確認します。
2. ブラシでは「描画色」と色ボックスが横一列に表示され、消しゴムでは描画色が表示されないことを確認します。
3. 比較が「なし／左右／上下」の3ボタンで1クリック切替できることを確認します。
4. ズームが「全体／−／%／＋」でコンパクトに表示され、25/50/75/100/150/200%を選択できることを確認します。
5. 自動選択のチェック表示が「連続／表示参照／AA」になっていることを確認します。
6. 色域選択で「基準／＋追加／−除外」「選択を確定／候補をクリア」が表示され、紫の候補と青の確定選択を区別できることを確認します。
7. レイヤーパネルに常時グレーアウトの「合成」が表示されず、不透明度だけが表示されることを確認します。
8. Forge選択画像を追加し、処理中にボタンが二重実行されず「画像を追加しています…」が表示されることを確認します。
9. 大きめの生成画像で、画像トレイ更新中のUI停止時間が0.7.7より短いことを体感確認します。
10. Quick RetouchのUndo/Redo、選択解除、反転→H/S、開始時に戻すが0.7.7から退行していないことを確認します。
